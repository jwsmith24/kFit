services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: kfit_db
      POSTGRES_USER: kfit
      POSTGRES_PASSWORD: kfit
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: